# ClawBY19 — 代码助手桌面程序 产品需求文档

> **运行环境：** Windows 桌面 EXE（C# 开发）
> **核心定位：** 通过自然语言聊天驱动的代码上传、云端发布与自动化测试一体化助手

---

## 系统架构概览

ClawBY19 是一个聊天驱动的桌面应用，用户通过自然语言对话触发三大核心功能。系统需要具备**意图识别**能力，从聊天语义中判断用户想要执行哪个功能，并自动完成参数收集、执行操作和结果反馈。

### 执行方式分类

| 功能模块 | 执行方 | 说明 |
|---------|--------|------|
| 功能一：代码上传 Git | 本地程序 | ClawBY19 在本地调用 Git 命令完成上传 |
| 功能二：代码发布云端 | 大模型 | 大模型远程登录云服务器执行发布操作 |
| 功能三：自检与测试 | 本地程序 | ClawBY19 在本地自动化遍历并测试目标程序 |

---

## 功能一：将本地代码上传到 Git 仓库

**执行方：** 本地程序

### 1.1 意图触发

- 当聊天语义匹配"把本地代码上传到 GitHub"或类似含义时触发。
- 支持 GitHub 及其他 Git 托管平台（GitLab、Gitee 等），系统根据目标仓库自动判断平台类型。

### 1.2 参数收集与窗体弹出

- 触发后，弹出参数填写窗体，包含以下必要字段：
  - 本地源代码目录路径
  - 目标 Git 仓库地址
  - 分支名称
  - 登录凭据（用户名、Token / SSH Key）
  - Commit 信息
- **凭据记忆机制：** 每次成功上传后，保存该仓库的登录信息和 Key。下次检测到相同仓库的上传操作时，自动填充上次保存的参数，用户确认即可执行。

### 1.3 智能文件过滤（上传前预处理）

- 上传前，ClawBY19 根据目录中的代码类型，调用大模型进行文件分析。
- **上传范围：** 仅上传源代码文件和依赖项配置文件（如 `package.json`、`requirements.txt`、`.csproj` 等）。
- **排除范围：** 编译产物（`bin/`、`obj/`、`dist/`）、调试文件（`.pdb`、`.log`）、IDE 配置（`.vs/`、`.idea/`）等非必要文件。
- 大模型应根据项目语言和框架动态判断过滤规则，而非使用固定白名单。

### 1.4 执行反馈

| 结果 | 显示方式 |
|------|---------|
| 全部成功 | 绿色文字显示成功信息 |
| 存在异常 | 红色文字显示错误详情 |

---

## 功能二：将 Git 仓库代码发布到云端

**执行方：** 大模型

### 2.1 意图触发

- 当聊天语义匹配"把代码发布到云端/服务器"或类似含义时触发。

### 2.2 发布流程

1. **获取云端连接信息：** 从聊天内容中提取云服务器的 IP 地址、用户名、密码（或密钥）。
2. **大模型远程登录：** 大模型通过 SSH 等方式访问云服务器。
3. **执行发布操作：** 按照用户在聊天中指定的要求，从 Git 仓库拉取代码并完成部署。

### 2.3 环境检查与自动配置

- 如果目标服务器环境未就绪，大模型需要：
  - 检查服务器环境，列出缺失或需要配置的项目清单。
  - 生成**一键安装脚本**，用户确认后执行。

### 2.4 凭据记忆机制

- 每次成功发布后，保存云端登录信息（IP、用户名、密码/密钥、发布路径等）。
- 下次聊天中提及相似的云端同步需求时，自动匹配已保存的信息，弹出确认窗体，用户确认后直接执行。

### 2.5 学习与自我优化

- **成功案例入库：** 每次成功执行的完整流程（参数、步骤、结果）存入学习库。
- **异常处理入库：** 执行异常时，大模型与用户的排查交流过程也存入学习库。
- 学习库用于后续相似场景的自动决策优化，实现系统自我升级。

---

## 功能三：新版本代码的自检与测试

**执行方：** 本地程序

### 3.1 意图触发

- 当聊天语义匹配"对程序进行测试"或类似含义时触发。

### 3.2 测试目标类型与参数收集

系统支持两种测试目标，通过追问确认：

#### 类型 A：Web 应用

- **追问内容：** 测试程序的访问 URL。
- **测试流程：**
  1. 根据用户提供的信息登录目标系统。
  2. 轮巡遍历所有功能模块和页面。
  3. 点击访问每个链接。
  4. 在所有可录入的控件中输入测试数据。
  5. 测试所有可点击的 Button 和 Link。

#### 类型 B：本地 EXE 程序

- **追问内容：** 程序的本地文件路径。
- **测试流程：**
  1. 启动目标 EXE 程序。
  2. 轮巡遍历所有窗体。
  3. 在所有可录入的控件中输入测试数据。
  4. 测试所有可点击的 Button 和 Link。

### 3.3 测试日志

每次测试需记录并保存以下信息：

| 字段 | 说明 |
|------|------|
| 菜单入口 | 测试功能所在的菜单路径 |
| 功能名称 | 被测试的具体功能 |
| 测试操作 | 执行了什么操作（点击、输入等） |
| 测试结果 | 通过 / 异常（含异常详情） |

测试日志持久化保存，支持后续查阅和对比分析。

---

## 通用设计要求

### 聊天意图识别

- 系统需要对用户的自然语言输入进行语义分析，自动识别属于哪个功能模块。
- 对于模糊或不完整的指令，系统应通过追问补全必要参数。

### 数据持久化

- Git 仓库凭据、云端登录信息、测试日志、学习库数据均需本地持久化存储。
- 支持按仓库地址 / 服务器 IP 进行历史记录的模糊匹配和自动填充。

### 反馈规范

- 成功信息统一使用**绿色文字**。
- 异常/错误信息统一使用**红色文字**。
- 所有操作执行前需用户确认，避免误操作。
